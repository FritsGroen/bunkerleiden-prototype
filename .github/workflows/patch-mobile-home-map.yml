name: Patch mobile home map first

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-mobile-home-map.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Refine mobile homepage and 3D framing
        run: python scripts/patch-mobile-home-3d-neat.py
      - name: Commit website update
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Refine mobile homepage map and 3D framing"
          git push
